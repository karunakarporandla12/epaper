
# Make the watcher use polling (reliable on Windows/OneDrive/network paths)
CHOKIDAR_USEPOLLING=true
WATCHPACK_POLLING=true
# (optional) shorter polling interval; increases CPU a bit but more responsive
CHOKIDAR_INTERVAL=100
# Ensure Fast Refresh is on
FAST_REFRESH=true
